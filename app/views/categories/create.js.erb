<% if @category.errors.any? %>
  $('#errorArea').html("<%= j render('shared/errors', object: @category)%>")
<% else %>
  $('#category').prepend("<%= j render('category_row', category: @category)%>")
  $('.close-modal').click()
<% end %>
