<% if @category.errors.any? %>
  $('#errorArea').html("<%= j render('shared/errors', object: @category)%>")
<% else %>
  $('#categoryRow_<%= @category.id %>').replaceWith("<%= j render('category_row', category: @category)%>")
  $('.close-modal').click()
<% end %>
